actions:
  run:
    desc: "Run a go program"
    cmds:
    - shell: go run .
  test:
    desc: "Run all go tests"
    cmds:
    - shell: go test ./... -cover
  build:
    desc: "Build all go apps"
    cmds:
    - shell: go build -o bin/ ./...